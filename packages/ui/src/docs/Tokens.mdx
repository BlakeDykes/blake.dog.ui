import { Meta } from "@storybook/addon-docs/blocks";
import {
  ColorTokens,
  SpaceTokens,
  TypeTokens,
  RadiusTokens,
  GradientTokens,
  FontFamilyTokens,

SEMANTIC_COLORS,
BRAND_COLORS,
BRAND_GRADIENTS,
BRAND_FONT_FAMILIES
} from "./TokenGallery";

<Meta title="Tokens" />

# Design tokens

Semantic custom properties generated from `tokens/*.json` by Style Dictionary.
Components only ever use this layer (or their own component tokens) — never the
numeric primitive scales. Flip the **Theme** toolbar control to see the
`[data-theme="dark"]` overrides apply.

## Color

<ColorTokens names={[...SEMANTIC_COLORS, ...BRAND_COLORS]} />

## Gradient

<GradientTokens names={[...BRAND_GRADIENTS]} />

## Space

<SpaceTokens />

## Typography

<FontFamilyTokens names={[...BRAND_FONT_FAMILIES]} />

## Type scale

<TypeTokens />

## Radius

<RadiusTokens />
